pipeline {
    agent { label 'prod' }

    triggers {
        githubPush()
    }

    stages {
        stage('Deploy') {
            when {
                branch 'master'
            }
            steps {
                sh 'cp -r * /opt/prod-app/'
            }
        }
    }
}
