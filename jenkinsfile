pipeline {
    ag**t { label 'test' }

    triggers **        githubPush()
    }

    s**ges {
        stage('Deploy') {
 **         steps {
                ** '''
                    echo "Cu**ent Workspace:"
                 ** pwd

                    echo "W**kspace Files:"
                  **ls -la

                    mkdir**p /opt/test-app

                **  cp -rv * /opt/test-app/

      **            echo "Files in Test A**:"
                    ls -la /op**test-app
                '''
    **      }
        }
    }
}
