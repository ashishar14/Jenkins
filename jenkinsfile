pipeline {
    agent { label 'test' }

    triggers {
        githubPush()
    }

    stages {
        stage('Deploy') {
            when {
                branch 'test'
            }
            steps {
                sh 'cp -r * /opt/test-app/'
            }
        }
    }
}
